@*    @model List<Category>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">

    <!-- Sidebar -->
    <div id="sidebar" class="d-flex flex-column align-items-center align-items-sm-start px-3 pt-2 text-white min-vh-100">
        <a href="/" class="d-flex align-items-center pb-3 mb-md-0 me-md-auto text-white text-decoration-none">
        </a>
        <button class="btn btn-dark" id="sidebarToggleBtn">
            <i class="fas fa-bars"></i>
        </button>
        <h2 style="color: white !important">Categories</h2>
        <a href="@Url.Action("Create", "Category")" class="btn btn-primary mt-3">
            <i class="fas fa-plus"></i> Add Category
        </a>

        <table class="table">
            @if (Model != null)
            {
                @foreach (var category in Model)
                {
                    if (category != null)
                    {
                        <tr>
                            <td>
                                <a asp-action="index" asp-controller="Folder" asp-route-categoryId="@category.Id" class="text-white">
                                    @category.Name
                                </a>
                            </td>
                        </tr>
                    }
                }
            }
        </table>
    </div>

    <style>
        #sidebar {
            transition: margin-left 0.3s;
        }

        #sidebarToggleBtn {
       
            top: 50px;
            left: 10px;
            z-index: 1000;
        }

        #sidebar.d-none {
            margin-left: -250px; /* Set the width of your sidebar */
        }

        #sidebar.d-sm-flex {
            margin-left: 0;
        }
    </style>

    <!-- Add jQuery and Bootstrap JS before your custom script -->
    <script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>

    <!-- Your existing code -->
    <script>
     $(document).ready(function () {
        console.log("Document ready function called");
        $("#sidebarToggleBtn").click(function () {
            $("#sidebar").toggleClass("d-none");
            $("#sidebar").toggleClass("d-sm-flex");
        });
    });

    </script>



*@
   @*<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
      <script src="https://code.jquery.com/jquery-3.6.4.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.5.2/dist/js/bootstrap.bundle.min.js"></script>

<div class="offcanvas offcanvas-start w-25" tabindex="-1" id="offcanvas" data-bs-keyboard="false" data-bs-backdrop="false">
    <div class="offcanvas-header">
        <h6 class="offcanvas-title d-none d-sm-block" id="offcanvasTitle">Menu</h6>
        <button type="button" class="btn-close text-reset" data-bs-dismiss="offcanvas" aria-label="Close"></button>
    </div>
    <div class="offcanvas-body px-0">
        <ul class="nav nav-pills flex-column mb-sm-auto mb-0 align-items-start" id="menu">
            <!-- Your menu items go here -->
            <li class="nav-item">
                <a href="#" class="nav-link text-truncate">
                    <i class="fs-5 bi-house"></i><span class="ms-1 d-none d-sm-inline">Home</span>
                </a>
            </li>
            <!-- Add other menu items as needed -->
        </ul>
    </div>
</div>

<div class="container-fluid">
    <div class="row">
        <div class="col min-vh-100 p-4">
            <!-- toggler -->
            <button class="btn float-end" data-bs-toggle="offcanvas" data-bs-target="#offcanvas" role="button">
                      <i class="fas fa-bars"></i>
            </button>


            <!-- Your content goes here -->
            <!-- Add more content as needed -->
        </div>
    </div>
</div>
*@
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">

 <div class="col-auto px-0">

            <div id="sidebar" class="collapse collapse-horizontal show border-end">
          <h3 style="color: black !important">Categories</h3>

        <a href="@Url.Action("Create", "Category")" class="btn btn-primary mt-3">
            <i class="fas fa-plus"></i> Add Category
        </a>
                <div id="sidebar-nav" class="list-group border-0 rounded-0 text-sm-start min-vh-100">
                    @if (Model != null)
                    {
                        foreach (var category in Model)
                        {
                            if (category != null)
                            {
                                <a href="@Url.Action("Index", "Folder", new { categoryId = category.Id,categoryName = category.Name })" class="list-group-item border-end-0 d-inline-block text-truncate" data-bs-parent="#sidebar">
                                    <i class="bi bi-folder"></i> <span>@category.Name</span>
                                </a>
                            }
                        }
                    }
                </div>
            </div>
        </div>
@*  <main class="col-auto col-md-2 ps-md-2 pt-2">
    <a href="#" data-bs-target="#sidebar" data-bs-toggle="collapse" class="border rounded-3 p-1 text-decoration-none">
        <i class="fas fa-bars fa-lg"></i> <!-- Reduced font size -->
    </a>
</main>*@

            
@*        </main>
*@ <script>
     $(document).ready(function () {
        console.log("Document ready function called");
        $("#sidebarToggleBtn").click(function () {
            $("#sidebar").toggleClass("d-none");
            $("#sidebar").toggleClass("d-sm-flex");
        });
    });

    </script>
